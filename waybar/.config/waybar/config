{
    "layer": "top",
    "position": "top",
    "height": 30,
    "modules-left": ["sway/workspaces", "sway/mode", "cpu", "memory"],
    "modules-center": ["clock", "custom/bluelight"],
    "modules-right": ["pulseaudio", "battery", "custom/vpn", "network", "tray"],
    
    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": false,
        "format": "{name}"
    },
    
    "clock": {
        "format": "{:%H:%M   %Y-%m-%d}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },
    
    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "BAT {capacity}%",
        "format-charging": "⚡ {capacity}%",
        "format-plugged": "🔌 {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },

    "custom/bluelight": {
    "exec": "pgrep -x gammastep > /dev/null && echo '🌙' || echo '☀️'",
    "format": "{}",
    "interval": 2,
    "on-click": "pkill gammastep || gammastep -l 32.7:-117.2 -t 6500:3500 -m wayland &"
    },
    
    "network": {
        "format-wifi": " {essid}",
        "format-disconnected": "⚠ Disconnected"
    },
    
    "pulseaudio": {
        "format": "VOL {volume}%",
        "format-muted": "🔇 Muted",
        "format-icons": {
            "default": ["", "", ""]
        },
        "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle"
    },
    
    "cpu": {
        "format": "CPU {usage}%",
        "tooltip": true,
        "interval": 2
    },
    
    "memory": {
        "format": "MEM {percentage}%",
        "tooltip-format": "Used: {used:0.1f}G / {total:0.1f}G",
        "interval": 2
    },
    
    "custom/vpn": {
        "exec": "~/.config/waybar/scripts/vpn-status.sh",
        "format": "{}",
        "interval": 5
    }
}